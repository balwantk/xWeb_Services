var HomeSubAppRouter = Backbone.Router.extend({
  routes: {
    "!home" : "home",
    "": "home",
    "!contact": "contact",
    "!team": "team"
  },
    
  home: function () {
    // Make a new VCO, pass the template, events and hit render. 
  },
  
  contact: function () {
    // Make a new VCO, pass the template, events and hit render. 
  },
  
  team: function () {
    // Make a new VCO, pass the template, events and hit render. 
  },
});

