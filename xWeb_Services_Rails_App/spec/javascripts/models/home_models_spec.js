describe("contact from visitor model", function() {
  it("should set the message object as visitor attrs", function() {
    // var message = {}
    // model.set(message)
    // expect(model.attributes)toBe(message);
  });
  
  it("should have the REST url defined", function() {
    // expect(model.url)toBe(the_correct_url);
  });
});