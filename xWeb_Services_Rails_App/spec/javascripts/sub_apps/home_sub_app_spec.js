describe("HomeSubApp", function() {
  it("should already be instantiated ", function() {
    expect(HomeSubApp).toBeTruthy;
  });
  
  it("should have instantiated the router", function() {
    expect(HomeSubAppRouter).toBeTruthy;
  });
  
  it("should have started backbone history", function() {
    /*
      TODO @dinesh, figure out how to write this spec!
    */
    
  });
});
