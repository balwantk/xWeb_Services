require "spec_helper"

describe MessageMailer do
  it "should send email with attributes described in the message hash"
  it "should throw error on failuer"
  it "should acknowledge on successful sent"
  
end
